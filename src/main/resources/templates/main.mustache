<!DOCTYPE HTML>
<html>
	<head>
	    <title>Hello Spring</title>
	    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
	    <link type="text/css" href="/styles/stylesUser.css">

	    <style type="text/css">
			* {
				box-sizing: border-box; 
			}

			html, body {
				width: 100%;
				height: 100%;
				margin: 0;
				padding: 0;
				font-family: 'Open Sans', sans-serif;
			}
			.menuName {
				width: 100%;
				text-align: center;
				border-bottom: 1px solid;

				font-family: Georgia, serif;
				font-style: italic;
				font-size: 20px;
				color: #5cc342;
			}

			h1 {
				margin: 0;
			}
			.menuForm {
				width: 600px;
				height: 325px;
				margin: 50px auto 0 auto;
				background: rgba(163, 240, 108, 0.5);
				padding: 20px;
				border-radius: 50px;
			}
			form {
				margin-top: 20px;
			}
			.formItem {
				width: 100%;
				padding: 10px 15px;
				border: 1px solid #5cc342;
				outline: none;
				margin: 0 0 10px 0;
			}
			.btn {
				width: 70%;
				margin-top: 10px;
				margin-left: 85px;
				border: none;
				background: #5cc342;
				color: #ffffff !important;
				font-weight: 100;
				padding: 15px;
				text-transform: uppercase;
				border-radius: 6px;
				display: inline-block;
				transition: all 0.3s ease 0s;
				text-decoration: none;

				font-family: Georgia, serif;
			}

			.btn:hover {
				cursor: pointer;
				color: #404040 !important;
				font-weight: 700 !important;
				letter-spacing: 3px;
				background: rgba(255,255,255,1);
				-webkit-box-shadow: 0px 10px 40px -10px rgba(0,0,0,0.57);
				-moz-box-shadow: 0px 10px 40px -10px rgba(0,0,0,0.57);
				transition: all 0.3s ease 0s;
			}
			.listName {
				width: 100%;
				text-align: center;
				border-bottom: 1px solid;

				font-family: Georgia, serif;
				font-style: italic;
				font-size: 25px;
				color: #f5a190;
				padding-bottom: 10px;
			}
			.listUsers {
				width: 600px;
				margin: 10px auto 0 auto;
				background: rgba(245, 161, 144, 0.5);
				padding: 20px;
				border-radius: 50px;
			}
			.gridMain, .gridName {
				display: grid;
				grid-template-columns: 40px 1fr 1fr 70px 1fr;
				border-bottom: 1px solid #f5a190;
				margin-bottom: 10px;
				text-align: center;
			}
			.gridName {
				background: #f5a190;
			}
	    </style>
	</head>
	<body>
		<div class="menuForm">
			<h1 class="menuName">Enter your data</h1>
		    <form method="post">
		        <input class="formItem" type="text" name="firstName" placeholder="Имя" required/>
		        <input class="formItem" type="text" name="secondName" placeholder="Фамилия" required/>
		        <input class="formItem" type="number" name="age" placeholder="Возраст" required/>
		        <input class="formItem" type="text" name="city" placeholder="Город" required/>
		        <button class="btn" type="submit">Add user</button>
		    </form>
		</div>
		<div class="listUsers">
			<div class="listName">List Users</div>
			<div class="gridName">
				<b>ID</b>
		        <span>First Name</span>
		        <i>Second Name</i>
		        <span>Age</span>
		        <span>City</span>
			</div>
			{{#users}}
			    <div class="gridMain">
			        <b>{{id}}</b>
			        <span>{{firstName}}</span>
			        <i>{{secondName}}</i>
			        <span>{{age}}</span>
			        <span>{{city}}</span>
			    </div>
			{{/users}}
		</div>
	</body>
</html>